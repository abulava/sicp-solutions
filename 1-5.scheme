;; exercise 1.5

(define (p) (p)) ;; бесконечная рекурсия, как мило

(define (test x y)
    (if (= x 0)
        0
        y))

;; (test 0 (p)) при applicative-evaluation order интерпретатор уйдет в бесконечную рекурсию на попытке вычислить значение (p)

0 ;; (test 0 (p)) при normal-evaluation order произойдет подстановка (if (= 0 0) 0 (p)), а вычисление значения этого выражения
  ;; уже не требует вычислить значение (p)